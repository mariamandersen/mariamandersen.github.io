Skybound mockup source supplied by Maria through Figma.

Run `npm ci` and `npm run build` in this directory to rebuild the self-hosted embed in `assets/skybound-mockup`. The portfolio loads that static build in its Skybound cover.
